import React from "react";

export function ProcessingOverlay({ show, message="Working on your betslip..." }:{ show:boolean; message?:string }) {
  if(!show) return null;
  return (
    <div className="fixed inset-0 z-[60] grid place-items-center bg-black/60 backdrop-blur">
      <div className="rounded-2xl border border-white/10 bg-neutral-900 p-5 w-[90%] max-w-sm text-center">
        <div className="mx-auto h-10 w-10 rounded-full border-2 border-white/20 border-t-white animate-spin" />
        <div className="mt-3 font-medium">⏳ {message}</div>
        <p className="text-[12px] opacity-70 mt-1">This usually takes a few seconds.</p>
      </div>
    </div>
  );
}
